<ul class="data-list">
	<li><h3>Were You Searching For?</h3></li>
	<% locations.each(function (location) { %>
		<li>
			<p id="<%= location.get("myId") %>" class="location">
				<%= location.get("street") %>
				<%= (location.city) ? location.get("city") + ", " : "" %>
				<%= location.get("state") %>
				<%= (location.zipcode) ? location.get("zipcode") + ", ": "" %>
				<%= location.get("country") %>
			</p>
		</li>
	<% }); %>
</ul>